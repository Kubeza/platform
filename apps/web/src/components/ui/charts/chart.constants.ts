export const DEFAULT_CHART_HEIGHT = 320;

export const MINI_CHART_HEIGHT = 72;

export const DEFAULT_STROKE_WIDTH = 2;

export const DEFAULT_RADIUS = 6;

export const DEFAULT_ANIMATION_DURATION = 400;

export const DEFAULT_GRID_DASHARRAY =
  "3 3";

export const DEFAULT_COLORS = [
  "hsl(var(--primary))",
  "hsl(var(--chart-1))",
  "hsl(var(--chart-2))",
  "hsl(var(--chart-3))",
  "hsl(var(--chart-4))",
  "hsl(var(--chart-5))",
] as const;